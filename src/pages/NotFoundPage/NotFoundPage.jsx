import "./NotFoundPage.scss"

function NotFoundPage() {
    return (
        <div className="not-found-page">
            <h1>404</h1>
            <p>The page you are looking for does not exist, or cannot be found.</p>
        </div>
    );
}

export default NotFoundPage;
